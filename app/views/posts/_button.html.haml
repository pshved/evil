-if btr = BUTTON_REGISTRY[button]
  %button{:type => 'button', :class => 'style', :name => btr[:name], :accesskey => btr[:accesskey], :title => btr[:title], :onclick => btr[:onclick]}
    -if btr[:html]
      !=btr[:html]
    -else
      =button
